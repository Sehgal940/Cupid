<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@4.0.0/fonts/remixicon.css"/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-700">
    <%if(nav){%>
        <div class="z-10 fixed w-full border-b-2 font-semibold text-xl text-white px-3 py-2 flex justify-between items-center bg-zinc-700">
            <h3 class="italic">.Cupid</h3>
            <div class="flex gap-10 items-center">
                <a class="px-3 py-1 text-sm  bg-red-500 text-white  rounded-lg " href="/logout">Logout</a>
            </div>
        </div>
    <%
    }%>